/**
 * Ink Observatory design note: the production experience is a self-contained
 * HTML document in client/index.html so the user can also download it as one file.
 */
export default function App() {
  return null;
}
